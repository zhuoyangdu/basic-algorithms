//
// Created by zy on 18-8-9.
//

#ifndef CODING_INSERT_SORT_H
#define CODING_INSERT_SORT_H

#include "../common/list_node.h"

// 插入排序
// 在要排序的一组数中，假定前n-1个数已经排好序，现在将第n个数插到前面的有序数列中，使得这n个数也是排好顺序的。
// 如此反复循环，直到全部排好顺序。

static ListNode* insertionSortList(ListNode* head) {
    ListNode* head0 = head;
    ListNode* current = head0;
    while (current!=NULL){
        
    }
}

#endif //CODING_INSERT_SORT_H
